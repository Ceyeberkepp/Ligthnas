{
  "name": "lightweight-ai-nas-os",
  "version": "0.12.0",
  "private": true,
  "type": "module",
  "description": "LightNAS storage, virtualization, container and infrastructure control plane",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "NAS_DEV=1 node --watch src/server.mjs",
    "test": "node --test",
    "check:platform": "node scripts/check-platform-contract.mjs"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=22"
  }
}
